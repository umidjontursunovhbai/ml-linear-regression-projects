# Insurance Linear Regression

## Dataset

Kaggle: https://www.kaggle.com/datasets/mirichoi0218/insurance

## Goal

Predict `charges` using `bmi`.

## Important

This dataset has outliers, especially in `charges`. Check outliers and remove extreme values using IQR before training.

Put `insurance.csv` inside `data/` before running the notebook.
